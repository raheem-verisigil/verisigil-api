# VeriSigilAI — GitHub Secret Scanning Configuration
# 
# The following patterns are DOCUMENTED PUBLIC TEST KEYS, not real credentials.
# They are intentionally present in the source as sandbox/demo API keys.

paths-ignore:
  - "main.py"

# Note: vs-sandbox-demo-2026b is a public sandbox key published in the
# VeriSigilAI documentation and used for the CLARA validation program.
# It grants access only to the sandbox endpoint with zero production authority.
# The corresponding secret is SANDBOX_API_KEY environment variable in Railway.
